# Bug Fixer Workflow Environment Variables

# Git Configuration
GIT_BRANCH=NMSTX-187-langgraph-orchestrator-migration
GIT_USER_NAME=Claude Code Agent
GIT_USER_EMAIL=claude@automagik-agents.ai

# Database Connection (use host.docker.internal for local development)
DATABASE_URL=postgresql://automagik_user:automagik_password@host.docker.internal:5432/automagik_agents

# Claude Configuration  
CLAUDE_MAX_TURNS=50
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# Workspace Configuration
WORKSPACE_DIR=/workspace/am-agents-labs
WORKFLOW_DIR=/workspace/workflow

# Memory Configuration
MEMORY_GROUP_ID=bug-fixes

# Optional: GitHub token for private repo access (will be mounted from host)
# GITHUB_TOKEN will be provided by container environment

# Optional: API keys for external services
# AM_API_KEY will be provided by container environment
# OPENAI_API_KEY will be provided by container environment